[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "OptimizationPackageJCC"
authors = [{name = "Jorge", email = "cobos03@icloud.com"}]
dynamic = ["version", "description"]
